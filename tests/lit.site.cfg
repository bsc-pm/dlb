# vim: ft=python
import sys
import litsupport.mixed

# name: The name of this test suite.
config.name = 'DLB'

if sys.version_info[0] < 3:
    # Python 2: just C and F90 tests using bets
    config.test_format = litsupport.bets.BetsTest()
    config.suffixes = ['.c', '.f90']
else:
    # Python 3: use a MixedTest class to decide between Python unittest or Bets
    config.test_format = litsupport.mixed.MixedTest()
    config.suffixes = ['.c', '.f90', '.py']

# test_source_root: The root path where tests are located.
config.test_source_root = os.path.dirname(__file__)
config.test_exec_root = os.getcwd()

# Bets location
config.bets = os.path.join(config.test_source_root, '..', 'scripts', 'bets')
